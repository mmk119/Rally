# Copy this file to .env and add your OpenAI key to power the chat bot with a real model.
# Without a key the bot still works using its built in keyword fallback.
# Note: VITE_ prefix is required by Vite to expose the variable to the browser.
VITE_OPENAI_API_KEY=
